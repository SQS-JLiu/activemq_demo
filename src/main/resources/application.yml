server:
  port: 8080

spring:
  activemq:
    broker-url: tcp://127.0.0.1:61616
    user: admin
    password: admin
  jms:
    pub-sub-domain: true   # false代表Queue  true代表Topic

#自定义队列名称
myqueue: boot-activemq-queue

mytopic: boot-activemq-topic
